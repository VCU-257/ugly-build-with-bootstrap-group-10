# Student Name: Steffen Niemeyer

## 1. My Assigned Work
I worked on the bucket overview page for our team, which can be found at `overview.html`. The page is just a basic page that shows you your bucket progress, the contributors, and list of transactions, that allows you to make a payment to the bucket. Everything is contained in `overview.html` more out of ease for grading than anything else.

## 2. Bootstrap Implementation
I used containers, cards, list-groups, and modals mainly to construct the page (and helper classes for each of those larger groups). Other classes I included from bootstrap was the progress bar, and various text, color, alignment, classes available for use. 

## 3. Technical Challenges & Solutions
At least for me, all this is stuff that I am used to/have done before in personal projects or in my job so there weren't any challenges for me. The hardest part of it was the functionality via Javascript, but still, I have a background with using these languages and tools so it wasn't too difficult.

## 4. AI / LLM Usage
I only used AI to do tedious tasks for me such as adding ID tags to each section. I.E. "Can you add ID tags to the html tags that are missing them." I did this because I planned to have a separate HTML file with custom CSS (for minor positional tweaks), but then re-read the assignment and realized it wasn't really needed. For everything else I used documentation and Google searches (Stack Overflow) to find answers.

## 5. Live Site Link
* **Live URL:** (Our Site)/overview.html *site has not been uploaded as the time of writing.*